#include <cs50.h>
#include <stdio.h>

// prototypes
check_sum(long num);

int main(void)
{
    long number = get_long("what's your credit card number: ")
    check sum(number);
}


bool check_sum(long num)
{
    int total = 0;
    while()
    {
        if(true)
        {
            total += num % 10;
        }

    }
}