#include <cs50.h>
#include <stdio.h>

// prototypes
check_sum(long num);

int main(void)
{
    long number = get_long("what's your credit card number: ")
    check sum(number);
}


bool check_sum(long num)
{
    int total = 0;
    bool var = true;
    while()
    {
        if( var == true)
        {
            total += num % 10;
            num/= 10;
            var = false;
        }
        else
        {
            buffer = num
        }

    }
}