#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(void)
{

    int letters = 0;
    int words = 0;
    int sentences = 0;

    string text = get_string("Text: ");
    for (int i=0; i < strlen(text); i++)
    {
        if(text[i] >='A' && <='a') || (text[i] >= 'Z' && >= 'z')
        {
            
        }
    }
}
